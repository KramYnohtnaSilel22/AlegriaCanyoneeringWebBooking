@model IEnumerable<AlegriaCanyoneeringWebBooking.WebUI.ViewModel.GuideAttendanceReportViewModel>
<link href="~/css/ReportsGuideAttendance.css" rel="stylesheet" />

@{
    var totalGuests = ViewBag.TotalGuests ?? 0;
    var totalRecords = ViewBag.TotalRecords ?? 0;
    var dateFrom = ViewBag.DateFrom as string ?? DateTime.Today.ToString("yyyy-MM-dd");
    var dateTo = ViewBag.DateTo as string ?? DateTime.Today.ToString("yyyy-MM-dd");
    var period = ViewBag.Period as string ?? "";
    var selectedArea = ViewBag.SelectedArea as string ?? "";
    var areas = ViewBag.Areas as List<string> ?? new List<string> { "Wonder Falls", "Kanlaob", "Kawasan Exit" };

    DateTime.TryParse(dateFrom, out var fromDt);
    DateTime.TryParse(dateTo, out var toDt);

    var rows = Model?.ToList() ?? new List<AlegriaCanyoneeringWebBooking.WebUI.ViewModel.GuideAttendanceReportViewModel>();
    int minRows = 31;
}

<div class="card mt-4 shadow">
    <!-- Card Header — hidden on print -->
    <div class="card-header bg-primary text-white d-flex flex-wrap justify-content-between align-items-center">
        <h5 class="mb-2 mb-md-0">
            <i class="bi bi-person-hiking me-2"></i>Guide Attendance Report
        </h5>
        <div class="d-flex flex-wrap align-items-center gap-2">
            <form method="get" asp-action="GuideAttendance" id="filterForm"
                  class="d-flex flex-wrap align-items-center gap-2">
                <div class="d-flex flex-column flex-sm-row align-items-sm-center gap-2">
                    <label class="fw-bold text-white">From:</label>
                    <input type="date" id="dateFrom" name="dateFrom" value="@dateFrom"
                           class="form-control form-control-sm" required />
                </div>
                <div class="d-flex flex-column flex-sm-row align-items-sm-center gap-2">
                    <label class="fw-bold text-white">To:</label>
                    <input type="date" id="dateTo" name="dateTo" value="@dateTo"
                           class="form-control form-control-sm" required />
                </div>
                <div class="d-flex flex-column flex-sm-row align-items-sm-center gap-2">
                    <label class="fw-bold text-white">Area:</label>
                    <select name="area" class="form-select form-select-sm" style="min-width:140px;">
                        <option value="" selected="@(string.IsNullOrEmpty(selectedArea) ? "selected" : null)">— All Areas —</option>
                        @foreach (var a in areas)
                        {
                            <option value="@a" selected="@(selectedArea == a ? "selected" : null)">@a</option>
                        }
                    </select>
                </div>
                <button type="submit" class="btn btn-light btn-sm mt-2 mt-sm-0">
                    <i class="bi bi-search"></i> Search
                </button>
            </form>
            <div class="btn-group mt-2 mt-sm-0">
                <button type="button" class="btn btn-info btn-sm dropdown-toggle"
                        data-bs-toggle="dropdown">
                    <i class="bi bi-printer"></i> Print
                </button>
                <ul class="dropdown-menu dropdown-menu-end">
                    <li>
                        <a class="dropdown-item" href="#" id="btnDownloadPDF">
                            <i class="bi bi-file-earmark-pdf"></i> Print as PDF
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item" href="#" id="btnDownloadExcel">
                            <i class="bi bi-file-earmark-excel"></i> Print as Excel
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Government Form Body -->
    <div class="card-body report-container" id="printArea">

        <!-- ===== GOVERNMENT FORM HEADER ===== -->
        <div class="gov-form-header text-center">
            <div class="gov-title">TOURIST TOUR GUIDE</div>
            <div class="gov-subtitle">Project</div>
        </div>

        <div class="gov-meta-row">
            <span class="gov-meta-left">LGU</span>
            <span class="gov-meta-center">MUNICIPALITY OF ALEGRIA</span>
            <span class="gov-meta-right">PERIOD&nbsp;&nbsp;<span id="displayPeriod">@period</span></span>
        </div>

        <!-- ===== MAIN TABLE ===== -->
        <div class="table-responsive">
            <table id="guideAttendanceTable" class="gov-table">
                <thead>
                    <tr>
                        <th class="col-seq"></th>
                        <th class="col-name">NAME</th>
                        <th class="col-datetime">DATE &amp; TIME</th>
                        <th class="col-route">ROUTE</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!rows.Any())
                    {
                        <tr>
                            <td colspan="4" class="text-center py-5 text-muted">
                                <i class="bi bi-inbox me-2" style="font-size:1.4rem;"></i><br />
                                <span class="fw-semibold">No data found</span>
                                @if (!string.IsNullOrEmpty(selectedArea))
                                {
                                    <span> for <strong>@selectedArea</strong></span>
                                }
                                <span> in the selected date range.</span>
                            </td>
                        </tr>
                    }
                    else
                    {
                        @for (int i = 0; i < minRows; i++)
                        {
                            if (i < rows.Count)
                            {
                                var item = rows[i];
                                <tr>
                                    <td class="col-seq text-center">@(i + 1)</td>
                                    <td class="col-name">@item.GuideName</td>
                                    <td class="col-datetime text-center">@item.Date @item.Time</td>
                                    <td class="col-route">@item.Route</td>
                                </tr>
                            }
                            else
                            {
                                <tr>
                                    <td class="col-seq text-center">@(i + 1)</td>
                                    <td class="col-name"></td>
                                    <td class="col-datetime"></td>
                                    <td class="col-route"></td>
                                </tr>
                            }
                        }
                    }
                </tbody>
            </table>
        </div>

        <!-- ===== FOOTER CERTIFICATION ===== -->
        <div class="gov-footer">
            <div class="gov-certified-row">
                <div class="gov-certified-left">
                    <div class="gov-certified-title">CERTIFIED</div>
                    <div class="gov-certified-body">
                        Each person whose name appears on this roll<br />
                        had rendered services for the time stated.
                    </div>
                    <div class="gov-sig-line">PICHEY L. LENDIO</div>
                    <div class="gov-sig-label">Name &amp; Signature of Timekeeper</div>
                </div>
                <div class="gov-certified-middle">
                    <div class="gov-certified-body gov-approved-text">Approved &nbsp;for Payment:</div>
                    <div class="gov-sig-line">HON. VERNA V. MAGALLON</div>
                    <div class="gov-sig-label">Name &amp; Signature of Approving Officer</div>
                </div>
                <div class="gov-certified-right">
                    <div class="gov-certified-body">
                        Each person whose name appears on the
                        above roll has been paid the amount
                        stated opposite his name after identifying
                        them.
                    </div>
                    <div class="gov-sig-line">JOSE WILSON C PATRIARCA</div>
                    <div class="gov-sig-label">Name &amp; Signature of Disbursing</div>
                </div>
            </div>
        </div>

        <p class="system-generated-text text-end small text-muted mt-2">
            <em>System Generated Report — Guide Attendance Summary</em>
        </p>

    </div><!-- /report-container -->
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/exceljs@4.4.0/dist/exceljs.min.js"></script>
    <script src="~/js/ReportsGuideAttendance.js" asp-append-version="true"></script>
}
