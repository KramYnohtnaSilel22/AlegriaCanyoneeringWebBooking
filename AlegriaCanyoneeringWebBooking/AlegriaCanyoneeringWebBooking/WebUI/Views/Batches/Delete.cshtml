@using AlegriaCanyoneeringWebBooking.Models
@model Batch
@{
    ViewData["Title"] = "Delete Batch";
}

@* This view is no longer used — delete is handled via AJAX modal in Index *@
@{
    Context.Response.Redirect("/Batches/Index");
}