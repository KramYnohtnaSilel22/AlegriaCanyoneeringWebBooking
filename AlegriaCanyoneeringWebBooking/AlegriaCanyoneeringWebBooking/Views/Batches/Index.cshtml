@model IEnumerable<AlegriaCanyoneeringWebBooking.Models.Batch>

@{
    ViewData["Title"] = "Batches";
}

<h2 class="mb-3">Batches</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New Batch</a>
</p>

<table class="table table-bordered table-striped">
    <thead class="table-dark">
        <tr>
            <th>Batch ID</th>
            <th>Operator</th>
            <th>Local Guests</th>
            <th>Foreign Guests</th>
            <th>Tour Guides</th>
            <th>Drivers</th>
            <th>Total Guests</th>
            <th>Arrival Date</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.BatchId</td>
                <td>@item.Operator?.BusinessName</td> <!-- shows Operator name if loaded -->
                <td>@item.NoOfLocalGuest</td>
                <td>@item.NoOfForeignGuest</td>
                <td>@item.NoOfTGuide</td>
                <td>@item.NoOfMDriver</td>
                <td>@item.TotalNoOfGuest</td>
                <td>@item.ArrivalDate</td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.BatchId" class="btn btn-info btn-sm">Details</a>
                    <a asp-action="Edit" asp-route-id="@item.BatchId" class="btn btn-warning btn-sm">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.BatchId" class="btn btn-danger btn-sm">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
